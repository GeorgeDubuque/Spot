package com.georgedubuque.spot

class Spot(spotName : String, spotType : String, spotRating : Int, spotImg : String) {

    var name = spotName
    var type = spotType
    var rating = spotRating
    var img = spotImg
}